---
id: git_commit_and_push
name: Git Commit & Push
description: Stage all changes, create a commit with a descriptive message, and push to remote.
version: "1.0"
category: git
tools: [git.status, git.commit, git.push]
example: "Commit my latest changes and push"
---

# Git Commit & Push

## Step 1
Goal: Check current git status to understand what changed.
Tools: git.status

## Step 2
Goal: Commit all changes with the provided message.
Tools: git.commit

## Step 3
Goal: Push the commit to the default remote branch.
Tools: git.push
